<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- PERMISOS JUSTIFICADOS (MVP §Fase 6 · cada uno con su porqué) -->

    <!-- Tablero de tasas (dolarapi/pydolarve), sala por servidor y respaldo: -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Detectar conectividad para el modo offline-first y degradar la UI: -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Cámara: foto de ticket en checkout y escáner de códigos de barras: -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Háptica de confirmación del escáner (feedback de lectura): -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Alertas locales (picos, metas, brecha, recordatorio) en API 33+: -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Sala P2P por Google Nearby (WiFi-Direct/Bluetooth/BLE) en API 31+: -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <!-- Descubrimiento P2P en API < 31 (Nearby lo exige para el scan): -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30"/>
    <!-- WiFi local: descubrimiento de anfitrión por broadcast en API 33+: -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"/>

    <application
        android:label="ValoraVE"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Shortcut interno: abrir el escáner directo (§12.2 quick_actions). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <data android:scheme="valorave" android:host="sala"/>
            </intent-filter>
        </activity>
        <!-- Familia de widgets de tasa (§12.2 · subclassing de BaseRatesWidget):
             BCV · Paralelo · Brecha — datos escritos por home_widget. -->
        <receiver
            android:name=".BcvWidgetProvider"
            android:exported="false"
            android:label="ValoraVE · Tasa BCV">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_bcv_info" />
        </receiver>
        <receiver
            android:name=".ParallelWidgetProvider"
            android:exported="false"
            android:label="ValoraVE · Paralelo">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_parallel_info" />
        </receiver>
        <receiver
            android:name=".GapWidgetProvider"
            android:exported="false"
            android:label="ValoraVE · Brecha">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_gap_info" />
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
