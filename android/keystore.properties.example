# Ejemplo de keystore.properties (NO versionar el real).
# Copia este archivo como android/keystore.properties y pon tus valores,
# o deja que el workflow de CI lo genere desde los secrets KEYSTORE_*.
storeFile=valorave-release.keystore
storePassword=********
keyAlias=valorave
keyPassword=********
